{
  "name": "ls-foundry",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=\"./packages/*\"",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "deploy": "pnpm run build:packages && pnpm -r publish --access public",
    "deploy:changed": "bash scripts/publish-changed.sh ${GIT_BEFORE:-HEAD~1} ${GIT_AFTER:-HEAD}",
    "deploy:dry-run": "pnpm run build:packages && pnpm -r publish --access public --dry-run",
    "deploy:nogit": "pnpm run build:packages && pnpm -r publish --access public --no-git-checks"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
